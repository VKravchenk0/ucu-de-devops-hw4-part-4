apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ucu-de-devops-hw4-part-4
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ucu-de-devops-hw4-part-4
                port: 
                  number: 80
